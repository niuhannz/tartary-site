import StudioPageShell from "@/components/StudioPageShell";

export default function StudioPage() {
  return (
    <StudioPageShell
      eyebrow="03 — Studio"
      title="Where the work gets made."
      subtitle="Gelatin Silver (film production), Swing Gang (virtual production), Niji (animation), and Tartary Game Studio — four divisions, one vision."
    />
  );
}
